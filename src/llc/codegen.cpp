#include "codegen.hpp"
#include <cassert>
#include <cstdint>
#include <cstdio>
#include <iostream>
#include <llvm/IR/Constants.h>
#include <llvm/IR/Instruction.h>
#include <llvm/IR/Instructions.h>
#include <llvm/IR/Module.h>
#include <llvm/Support/TypeSize.h>
#include <llvm/Support/raw_ostream.h>

using namespace std;
using namespace llvm;

Codegen::Codegen(ostream &os) : os(os) {
  os << "\t; Generated by ZDK IR compiler\n\n";
}

std::any Codegen::visit_module(Module &module) {
  this->module = &module;
  for (auto &func : module) {
    if (func.isDeclaration()) {
      continue;
    }
    visit_function(func);
  }
  return nullptr;
}

std::any Codegen::visit_function(Function &function) {
  os << "\t.global " << function.getName().str() << "\n";
  os << "\t.type " << function.getName().str() << ", @function\n";
  os << function.getName().str() << ":\n";

  generate_prologue(function);

  for (auto &block : function) {
    for (auto &inst : block) {
      visit_instruction(inst);
    }
  }

  return {};
}

std::any Codegen::visit_instruction(Instruction &inst) {
  comment_instruction(inst);

  switch (inst.getOpcode()) {
  case Instruction::Load:
    os << "\t;   (Aliased to offset " << ctx.offsets[&inst] << ")\n";
    break;
  case Instruction::Store:
    visit_store(cast<StoreInst>(&inst));
    break;
  case Instruction::Ret:
    visit_return(cast<ReturnInst>(&inst));
    break;
  }

  return {};
}

void Codegen::generate_prologue(Function &function) {
  // TODO: Collect arguments

  int local_offset = 0;
  for (auto &block : function) {
    for (auto &inst : block) {
      if (auto *alloca = dyn_cast<AllocaInst>(&inst)) {
        ctx.offsets[alloca] = local_offset;
        local_offset -= module->getDataLayout().getTypeAllocSize(alloca->getAllocatedType());
      } else if (auto *load = dyn_cast<LoadInst>(&inst)) {
        auto *ptr = load->getPointerOperand();
        ctx.offsets[&inst] = ctx.offsets[ptr];
      } else if (!inst.getType()->isVoidTy()) {
        ctx.offsets[&inst] = local_offset;
        local_offset -= module->getDataLayout().getTypeAllocSize(inst.getType());
      }
    }
  }

  // TODO: Clause for arguments
  if (local_offset) {
    os << "\tpush ix\n";
    os << " \tld ix, 0\n";
    os << " \tadd ix, sp\n";

    os << " \tld hl, " << local_offset << "\n";
    os << " \tadd hl, sp\n";
    os << " \tld sp, hl\n";
  }

  return;
}

void Codegen::comment_instruction(Instruction &inst) {
  string str;
  raw_string_ostream rso(str);
  inst.print(rso);
  os << "\t; " << str << "\n";
}

string Codegen::get_ix_offset(Value *value, int offset) {
  string str = "(ix";

  int ix_offset = ctx.offsets[value];
  if (ix_offset < 0)
    str += '-';
  else
    str += '+';
  str += to_string(abs(ix_offset) + offset) + ')';

  return str;
}

void Codegen::visit_return(ReturnInst *inst) {
  // TODO: Load return value
  if (!ctx.offsets.empty()) {
    os << "\tld sp, ix\n";
    os << "\tpop ix\n";
  }
  os << "\tret\n";
}

void Codegen::visit_store(StoreInst *inst) {
  auto *src = inst->getValueOperand();
  auto *dst = inst->getPointerOperand();
  Type *type = src->getType();
  TypeSize size = module->getDataLayout().getTypeAllocSize(type);

  if (auto *constant = dyn_cast<ConstantInt>(src)) {
    int64_t value = constant->getSExtValue();
    for (uint64_t i = 0; i < size; i++) {
      os << "\tld " << get_ix_offset(dst, i) << ", "
         << ((value >> (i * 8)) & 0xff) << "\n";
    }
  } else {
    // TODO: Implement
    throw runtime_error("store not implemented");
  }
}
